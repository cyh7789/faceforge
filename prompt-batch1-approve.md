核准。你的設計提案全部通過，包括：grit/風霜命名、FNV-1a 卡片 ID、兩眼皮共用垂簾聽政者（15 收集職業）、錯誤碼映射（DLQ → face_too_small）、以及移除 next/font/google 改系統字型的 scaffold 修正。

按你的計畫完整實作到底：engine 全模組 + tests + /api/analyze（mock + live）+ fixtures 搬運 + git 兩段 commit。做完直接給我 `npm test` 全量輸出與 `npm run build` 結果，不要再停下來等核准。
